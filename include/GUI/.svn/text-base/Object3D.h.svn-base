namespace System
{
namespace Gui
{
namespace Media3D
{

class GUIEXT Object3D : public Element
{
protected:

	CTOR Object3D(DependencyClass* depClass);

public:

	/*
	IRootVisual* GetRoot()
	{
		return m_root;
	}

	void SetRoot(IRootVisual* root)
	{
		m_root = root;
		OnSetRoot(root);
	}

	virtual void OnSetRoot(IRootVisual* root)
	{
	}
	*/

	Document3D* m_document;
//	String m_name;

	DEPENDENCY_CLASS_ABSTRACT(Object3D, Element)

		/*
private:

	IRootVisual* m_root;
	*/
};

}	// Media3D
}	// Gui
}	// System

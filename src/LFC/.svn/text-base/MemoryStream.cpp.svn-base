#include "stdafx.h"
#include "LFC.h"

namespace System
{
namespace IO
{

MemoryStream::MemoryStream(void* buffer, size_t size) : m_buffer((byte*)buffer), m_size(size), m_pos(0)
{
}

MemoryStream::MemoryStream(const void* buffer, size_t size) : m_buffer((byte*)buffer), m_size(size), m_pos(0)
{
}

size_t MemoryStream::Read(void* pv, size_t cb)
{
	ASSERT(0);
	return 0;
}

size_t MemoryStream::Write(const void* pv, size_t cb)
{
	ASSERT(0);
	return 0;
}

uint64 MemoryStream::Seek(int64 offset, SeekOrigin origin)
{
	ASSERT(0);
	return 0;
}

uint64 MemoryStream::GetSize()
{
	return m_size;
}

}	// IO
}	// System

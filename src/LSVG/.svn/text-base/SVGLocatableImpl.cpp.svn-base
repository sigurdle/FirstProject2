#include "stdafx.h"
#include "LSVG2.h"

#include "SVGLocatableImpl.h"

#include "PSVGElement.h"

namespace System
{
namespace Web
{

#if 0
HRESULT SVGLocatableImplImpl::getRBBox(CLDOMElementImplImpl* pElement, /*[out,retval]*/ ILSVGRect** pVal)
{
	ASSERT(0);
	return S_OK;
}
#endif

ISVGRect* SVGLocatableImplImpl::getBBox()
{
	ASSERT(0);
	return NULL;
#if 0
	if (pVal == NULL) return E_POINTER;
	*pVal = NULL;

	CComQIPtr<CLDOMDocumentImplImpl> documentImpl = pElement->m_ownerDocument;
	if (documentImpl->m_pViews.GetSize() > 0)
	{
		ASSERT(0);
#if 0
		return documentImpl->m_pViews[0]->svgGetBBox(CComQIPtr<ILSVGElement>(pElement), pVal);
#endif
	}
#endif
}

}	// w3c
}

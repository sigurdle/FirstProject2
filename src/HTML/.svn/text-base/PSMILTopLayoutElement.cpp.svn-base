#include "stdafx.h"
#include "HTML2.h"

#include "PSMILTopLayoutElement.h"

//#include "LSMILRootLayoutElement.h"
#include "SMILTopLayoutElement.h"

namespace System
{
namespace Web
{

// virtual
void PSMILTopLayoutElement::Render(Graphics::Bitmap* pBgBitmap, Gui::ManagedRetainedRenderContext, double scaleX, double scaleY)
{
//	CLSMILRegionElement* psmilElement = static_cast<CLSMILRegionElement*>(m_pNode);

	ASSERT(0);
#if 0
	UPOSITION pos = m_childList.GetHeadPosition();
	while (pos)
	{
		CPSMILElementImpl* pElement = (CPSMILElementImpl*)m_childList.GetNext(pos);

		pElement->Render(pBgBitmap, pGraphics, scaleX, scaleY);
	}
#endif
}

/*
LRESULT CPSMILTopLayoutElement::OnPaint(UINT uMsg, WPARAM wParam, LPARAM lParam, BOOL& bHandled)
{
	return 0;
}

LRESULT CPSMILTopLayoutElement::OnSize(UINT uMsg, WPARAM wParam, LPARAM lParam, BOOL& bHandled)
{
	return 0;
}
*/

}	// Web
}
